import { LikedByPipePipe } from './liked-by-pipe.pipe';

describe('LikedByPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LikedByPipePipe();
    expect(pipe).toBeTruthy();
  });
});
